<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.area.areabackend.dao.SwiperDao">
    <select id="querySwipers" resultType="com.example.area.areabackend.entity.Swiper">
        SELECT id, title, thumbnail, source, url, updateTime
        FROM db_swiper
    </select>
</mapper>