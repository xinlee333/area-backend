<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.area.areabackend.dao.SpecialDao">
    <select id="querySpecials" resultType="com.example.area.areabackend.entity.Special">
        SELECT id, title, thumbnail, type, url
        FROM db_special
    </select>
</mapper>